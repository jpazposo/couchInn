"use strict";
angular.module('login').controller(
    'loginController',
    [
        '$scope',
        function ($scope) {
          console.log('se cargó el controller loginController');
        }
    ]
);
